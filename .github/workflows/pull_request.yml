name: Print Pull Request Context
on: workflow_dispatch
jobs:
  print:
    runs-on: windows-latest
    steps:
      - name: Print context
        run: |
          echo "${{ toJson(github)}}"
          Write-Host "${{ toJson(github)}}"
